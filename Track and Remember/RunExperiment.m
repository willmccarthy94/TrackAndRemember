% RunExperiment
% This function should be called whenever th 

function RunExperiment()

    % Load experimental variables struct
    load('ExperimentVars.mat');
    
    % Initialize experiment, data structures etc.
    % Check whether file with that participant number already exists.
    
    % Loop over blocktypes
    
    % Loop over trials
    
    % Save data with save('trackremember_ppt_XXX.mat','data') but with
    % correct participant number. 
    
    
end